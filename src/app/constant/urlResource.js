const HttpMethod = {
    POST: "POST",
    GET: "GET",
    PUT: "PUT",
    DELETE: "DELETE"
}

const host = "http://localhost:8080";

const paths = {
    HOME_RESOURCES: "/api/v1/home"
}

export {
    HttpMethod,
    paths,
    host
}